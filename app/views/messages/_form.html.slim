div.well
  = simple_form_for([@emergency, @message]) do |f|
    = f.input :text, rows: 3
    - if emergency_policy(@emergency).owner?
      = f.input :claim_closed, as: :boolean, class: 'btn btn-primary'
    = f.button :submit

javascript:
  $('#message_text').froalaEditor({
    paragraphFormat: {
      N: 'Normal',
      H5: 'Heading',
      PRE: 'Code'
    },
    pastePlain: true
  })
